import crypto from 'node:crypto'; import fs from 'node:fs'; import path from 'node:path';
const fsp = fs.promises; const STATE_SCHEMA = 'swir.peer-authorization-provisioning-state/0.1'; const REPORT_SCHEMA = 'swir.peer-authorization-provisioning-report/0.1';
const MANAGED_DIRECTORIES = Object.freeze([{ path: '/usr/libexec/swir', mode: 0o755 }, { path: '/usr/lib/systemd/system', mode: 0o755 }, { path: '/usr/lib/systemd/system-preset', mode: 0o755 }, { path: '/usr/lib/tmpfiles.d', mode: 0o755 }, { path: '/var/lib/swir/image', mode: 0o700 }]);
const ARTIFACTS = Object.freeze([
  { id: 'peer-broker', source: 'system/ipc/swir-peer-authorization-broker.py', path: '/usr/libexec/swir/swir-peer-authorization-broker', mode: 0o755, required: ['SO_PEERCRED', 'DEFAULT_PKCHECK = "/usr/bin/pkcheck"', 'DEFAULT_LOGINCTL = "/usr/bin/loginctl"', 'uid <= 0'] },
  { id: 'peer-socket', source: 'system/ipc/swir-peer-authorization.socket', path: '/usr/lib/systemd/system/swir-peer-authorization.socket', mode: 0o644, required: ['ListenStream=/run/swir/peer-authorization.sock', 'SocketMode=0666', 'Accept=no', 'MaxConnections=64'] },
  { id: 'peer-service', source: 'system/ipc/swir-peer-authorization.service', path: '/usr/lib/systemd/system/swir-peer-authorization.service', mode: 0o644, required: ['ExecStart=/usr/libexec/swir/swir-peer-authorization-broker', 'NoNewPrivileges=yes', 'ProtectSystem=strict', 'RestrictAddressFamilies=AF_UNIX', 'ReadWritePaths=/run/swir'] },
  { id: 'peer-tmpfiles', source: 'system/ipc/swir-peer-authorization.tmpfiles.conf', path: '/usr/lib/tmpfiles.d/swir-peer-authorization.conf', mode: 0o644, required: ['d /run/swir 0755 root root - -'] },
  { id: 'peer-preset', source: 'system/ipc/90-swir-peer-authorization.preset', path: '/usr/lib/systemd/system-preset/90-swir-peer-authorization.preset', mode: 0o644, required: ['enable swir-peer-authorization.socket'] }
]);
const STATE_PATH = '/var/lib/swir/image/peer-authorization-provisioning-state.json';
function fail(code, message, cause) { const error = new Error(message, cause ? { cause } : undefined); error.name = 'SystemPeerAuthorizationProvisioningError'; error.code = code; throw error; }
function assert(condition, code, message) { if (!condition) fail(code, message); }
function digest(data) { return crypto.createHash('sha256').update(data).digest('hex'); }
function target(rootfs, imagePath) { assert(typeof imagePath === 'string' && imagePath.startsWith('/') && imagePath !== '/' && path.posix.normalize(imagePath) === imagePath, 'INVALID_IMAGE_PATH', `invalid image path ${imagePath}`); const resolved = path.resolve(rootfs, `.${imagePath}`); assert(resolved.startsWith(`${rootfs}${path.sep}`), 'IMAGE_PATH_ESCAPE', `image path escapes rootfs: ${imagePath}`); return resolved; }
async function safeRoot(rootfs) { assert(typeof rootfs === 'string' && path.isAbsolute(rootfs), 'ROOTFS_PATH_REQUIRED', 'rootfs must be an explicit absolute path'); const resolved = path.resolve(rootfs); assert(resolved !== path.parse(resolved).root, 'REAL_ROOT_TARGET_FORBIDDEN', 'refusing to provision live filesystem root'); const stat = await fsp.lstat(resolved).catch(error => fail('ROOTFS_UNAVAILABLE', `rootfs unavailable: ${resolved}`, error)); assert(stat.isDirectory() && !stat.isSymbolicLink(), 'ROOTFS_INVALID', 'rootfs must be a real non-symlink directory'); assert(await fsp.realpath(resolved) === resolved, 'ROOTFS_SYMLINKED', 'rootfs must resolve exactly to requested directory'); return resolved; }
async function assertNoSymlinkAncestors(rootfs, destination) { const relative = path.relative(rootfs, destination); assert(relative && !path.isAbsolute(relative) && relative !== '..' && !relative.startsWith(`..${path.sep}`), 'IMAGE_PATH_ESCAPE', 'managed path escaped rootfs'); let current = rootfs; const parts = relative.split(path.sep); for (let i = 0; i < parts.length; i += 1) { current = path.join(current, parts[i]); try { const stat = await fsp.lstat(current); assert(!stat.isSymbolicLink(), 'IMAGE_PATH_SYMLINK_ANCESTOR', `managed image path traverses symlink: ${current}`); if (i < parts.length - 1) assert(stat.isDirectory(), 'IMAGE_PATH_ANCESTOR_INVALID', `non-directory ancestor: ${current}`); } catch (error) { if (error?.code === 'ENOENT') return; throw error; } } }
async function trustedSource(sourceRoot, relative) { assert(typeof sourceRoot === 'string' && path.isAbsolute(sourceRoot), 'SOURCE_ROOT_REQUIRED', 'sourceRoot must be absolute'); const root = await fsp.realpath(sourceRoot); const source = path.resolve(root, relative); assert(source.startsWith(`${root}${path.sep}`), 'SOURCE_ESCAPE', `source escaped repository: ${relative}`); const stat = await fsp.lstat(source); assert(stat.isFile() && !stat.isSymbolicLink() && await fsp.realpath(source) === source, 'SOURCE_UNTRUSTED', `source must be a regular non-symlink file: ${relative}`); return source; }
async function atomicWrite(destination, data, mode) { try { const existing = await fsp.lstat(destination); assert(existing.isFile() && !existing.isSymbolicLink(), 'DESTINATION_UNTRUSTED', `unsafe destination ${destination}`); } catch (error) { if (error?.code !== 'ENOENT') throw error; } const temporary = path.join(path.dirname(destination), `.${path.basename(destination)}.${process.pid}.${crypto.randomBytes(6).toString('hex')}.tmp`); try { await fsp.writeFile(temporary, data, { mode, flag: 'wx' }); await fsp.chmod(temporary, mode); await fsp.rename(temporary, destination); } finally { await fsp.rm(temporary, { force: true }).catch(() => {}); } }
async function inspect(destination, expectedMode, ownerUid, directory) { try { const stat = await fsp.lstat(destination); if (stat.isSymbolicLink()) return { trusted: false, reason: 'symlink' }; const typeOk = directory ? stat.isDirectory() : stat.isFile(); const mode = stat.mode & 0o777; const ownerOk = ownerUid === null || typeof stat.uid !== 'number' || stat.uid === ownerUid; return { trusted: typeOk && mode === expectedMode && ownerOk, typeOk, mode, ownerUid: stat.uid ?? null, ownerOk }; } catch (error) { return { trusted: false, reason: error?.code || 'probe-failed' }; } }
export async function stagePeerAuthorizationFoundation({ rootfs, sourceRoot, production = false, clock = () => new Date().toISOString() } = {}) { const root = await safeRoot(rootfs); const ownerUid = production ? 0 : (typeof process.getuid === 'function' ? process.getuid() : null); assert(Number.isInteger(ownerUid) && ownerUid >= 0, 'OWNER_UID_UNAVAILABLE', 'cannot determine owner uid'); if (production) { assert(typeof process.getuid === 'function' && process.getuid() === 0, 'PRODUCTION_REQUIRES_ROOT', 'production provisioning requires root'); const rootStat = await fsp.lstat(root); assert(rootStat.uid === 0 && (rootStat.mode & 0o022) === 0, 'PRODUCTION_ROOTFS_UNTRUSTED', 'production rootfs must be root-owned and not group/world writable'); }
  for (const entry of MANAGED_DIRECTORIES) { const destination = target(root, entry.path); await assertNoSymlinkAncestors(root, destination); try { const stat = await fsp.lstat(destination); assert(stat.isDirectory() && !stat.isSymbolicLink(), 'DESTINATION_UNTRUSTED', `unsafe managed directory ${entry.path}`); } catch (error) { if (error?.code !== 'ENOENT') throw error; await fsp.mkdir(destination, { recursive: true, mode: entry.mode }); } await fsp.chmod(destination, entry.mode); if (production) await fsp.chown(destination, 0, 0); }
  const artifacts = []; for (const entry of ARTIFACTS) { const source = await trustedSource(sourceRoot, entry.source); const content = await fsp.readFile(source); const text = content.toString('utf8'); for (const token of entry.required) assert(text.includes(token), 'SOURCE_CONTRACT_MISMATCH', `${entry.id} is missing required token: ${token}`); const destination = target(root, entry.path); await assertNoSymlinkAncestors(root, destination); await fsp.mkdir(path.dirname(destination), { recursive: true }); await assertNoSymlinkAncestors(root, destination); await atomicWrite(destination, content, entry.mode); if (production) await fsp.chown(destination, 0, 0); artifacts.push({ id: entry.id, path: entry.path, mode: entry.mode, bytes: content.length, sha256: digest(content) }); }
  const state = { schema: STATE_SCHEMA, production, ownerUid, generatedAt: clock(), artifacts, socketActivationPreset: true, runtimeKeyEmbeddedInImage: false, bootableImageClaim: false }; const stateFile = target(root, STATE_PATH); await assertNoSymlinkAncestors(root, stateFile); await atomicWrite(stateFile, Buffer.from(`${JSON.stringify(state, null, 2)}\n`), 0o600); if (production) await fsp.chown(stateFile, 0, 0); const report = await verifyPeerAuthorizationFoundation({ rootfs: root, production, expectedOwnerUid: ownerUid }); assert(report.ready, 'PROVISIONING_VERIFICATION_FAILED', `peer authorization staging failed: ${report.blockers.join(', ')}`); return Object.freeze({ ...report, staged: true, state }); }
export async function verifyPeerAuthorizationFoundation({ rootfs, production = false, expectedOwnerUid = production ? 0 : (typeof process.getuid === 'function' ? process.getuid() : null) } = {}) { const root = await safeRoot(rootfs); const checks = []; for (const entry of MANAGED_DIRECTORIES) { const destination = target(root, entry.path); let ancestorsSafe = true; try { await assertNoSymlinkAncestors(root, destination); } catch { ancestorsSafe = false; } const result = await inspect(destination, entry.mode, expectedOwnerUid, true); checks.push({ id: `dir:${entry.path}`, required: true, passed: ancestorsSafe && result.trusted, detail: result }); }
  let state = null; const stateFile = target(root, STATE_PATH); try { state = JSON.parse(await fsp.readFile(stateFile, 'utf8')); } catch {} checks.push({ id: 'state-shape', required: true, passed: state?.schema === STATE_SCHEMA && Array.isArray(state?.artifacts), detail: { schema: state?.schema ?? null } }); checks.push({ id: 'runtime-key-not-baked-into-image', required: true, passed: state?.runtimeKeyEmbeddedInImage === false, detail: { embedded: state?.runtimeKeyEmbeddedInImage ?? null } }); const recorded = new Map((state?.artifacts || []).map(item => [item?.id, item]));
  for (const entry of ARTIFACTS) { const destination = target(root, entry.path); let ancestorsSafe = true; try { await assertNoSymlinkAncestors(root, destination); } catch { ancestorsSafe = false; } const result = await inspect(destination, entry.mode, expectedOwnerUid, false); let integrity = false; let requiredTokens = false; try { const content = await fsp.readFile(destination); const record = recorded.get(entry.id); integrity = Boolean(record && record.path === entry.path && record.mode === entry.mode && record.bytes === content.length && record.sha256 === digest(content)); const text = content.toString('utf8'); requiredTokens = entry.required.every(token => text.includes(token)); } catch {} checks.push({ id: `file:${entry.id}`, required: true, passed: ancestorsSafe && result.trusted && integrity && requiredTokens, detail: { ...result, ancestorsSafe, integrity, requiredTokens } }); }
  const stateStat = await inspect(stateFile, 0o600, expectedOwnerUid, false); checks.push({ id: 'state-file', required: true, passed: stateStat.trusted, detail: stateStat }); const blockers = checks.filter(check => check.required && !check.passed).map(check => check.id); return Object.freeze({ schema: REPORT_SCHEMA, ready: blockers.length === 0, production, bootableImageClaim: false, socketActivationPreset: true, runtimeKeyEmbeddedInImage: false, checks: Object.freeze(checks), blockers: Object.freeze(blockers) }); }
export const PeerAuthorizationProvisioningPolicy = Object.freeze({ schema: 'swir.peer-authorization-provisioning-policy/0.1', liveRootTargetAllowed: false, symlinkDestinationsAllowed: false, runtimeKeyEmbeddedInImage: false, socketEnabledByPreset: true, bootableImageClaim: false, productionRequiresRoot: true });
