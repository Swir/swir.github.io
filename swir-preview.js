(()=>{const cleanupLegacy=async()=>{try{if('serviceWorker'in navigator){const regs=await navigator.serviceWorker.getRegistrations();await Promise.all(regs.map(reg=>reg.unregister()));}if('caches'in window){const keys=await caches.keys();await Promise.all(keys.filter(key=>key.startsWith('swir-os-')).map(key=>caches.delete(key)));}}catch{}};cleanupLegacy();const $=(q,r=document)=>r.querySelector(q),$$=(q,r=document)=>[...r.querySelectorAll(q)];const modes={desktop:{title:'Desktop Edition',copy:'A native Windows desktop bridge today, designed to evolve into the full SWIR desktop experience with real files, processes, networking, updates and bundled runtime components.'},mobile:{title:'Mobile Experience',copy:'A compact SWIR interface concept for phones: fast launcher, system apps, notifications and the same visual identity adapted for touch.'}};$$('[data-mode]').forEach(btn=>btn.addEventListener('click',()=>{$$('[data-mode]').forEach(b=>b.classList.toggle('active',b===btn));const mode=btn.dataset.mode;$('#device-desktop').hidden=mode!=='desktop';$('#device-mobile').hidden=mode!=='mobile';$('#preview-title').textContent=modes[mode].title;$('#preview-copy').textContent=modes[mode].copy;}));const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('visible')}),{threshold:.12});$$('.reveal').forEach(el=>io.observe(el));const clock=()=>{const d=new Date();$$('[data-clock]').forEach(el=>el.textContent=d.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}));$('#mobile-date')&&($('#mobile-date').textContent=d.toLocaleDateString([],{weekday:'long',month:'long',day:'numeric'}));};clock();setInterval(clock,30000);$$('.app-launch').forEach(el=>el.addEventListener('click',()=>{const name=el.dataset.app||'SWIR App';const win=$('#demo-window');if(win){win.querySelector('b').textContent=name;win.querySelector('.file-list').innerHTML=`<b>${name}</b><br>Preview surface ready<br>SWIR OS design concept`;win.animate([{transform:'scale(.96)',opacity:.65},{transform:'scale(1)',opacity:1}],{duration:180});}}));$('#year').textContent=new Date().getFullYear();})();